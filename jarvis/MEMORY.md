# Long-term memory (JARVIS)

Curated facts the agent can recall via `memory_search`. Add preferences, decisions, and durable notes here. Daily notes go in `memory/YYYY-MM-DD.md`.

See [scripts/MEMORY_AND_SPEED.md](../scripts/MEMORY_AND_SPEED.md) for enabling OpenClaw memory + compaction.
