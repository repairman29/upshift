# Copy to .env and fill in. Used by Cursor (JARVIS rule) and scripts/call-jarvis.js
# cp .env.example .env   then edit .env

# Your Supabase project ref (from Supabase Dashboard URL: https://app.supabase.com/project/YOUR_REF)
JARVIS_EDGE_URL=https://YOUR_REF.supabase.co/functions/v1/jarvis

# UpshiftAI API key (from https://upshiftai.dev/pricing or platform dashboard)
UPSHIFTAI_API_KEY=uai_pro_xxx

# Radar Pro: upload scan reports from CLI (upshift scan --report out.json --upload)
# Get your upload token from the Radar Pro dashboard; API URL = your Supabase functions base
UPSHIFT_RADAR_UPLOAD_URL=https://YOUR_REF.supabase.co/functions/v1/radar-upload
UPSHIFT_RADAR_TOKEN=your_upload_token_uuid

# Team/audit: CLI posts events when these are set (see docs/team-features.md)
UPSHIFT_AUDIT_URL=https://YOUR_REF.supabase.co/functions/v1/audit-events
# UPSHIFT_ORG=your_org_id
# UPSHIFT_API_TOKEN=optional_bearer_token
